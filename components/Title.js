import React from "react";
import { Text } from "@chakra-ui/react";

export const Margin = function (props) {

    return (
        <Text fontSize="big" fontWeight={600} color="argila.500">
            {props.text}
        </Text>
)
}
export default Margin